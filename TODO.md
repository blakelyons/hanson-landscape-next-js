# TODO

- Swap custom exported SVG icons (currently in `public/images/home/`) for Iconify equivalents where a matching glyph exists, using `src/components/ui/icon.tsx` + `src/lib/icon-config.ts`. Not urgent.
