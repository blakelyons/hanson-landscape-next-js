# Mission: Next.js + GSAP

## Why
Blake already knows Next.js well but wants a refresher on rusty areas and to level up his GSAP skills — applied directly to real client work on the Hanson Landscape site rebuild, not abstract exercises.

## Success looks like
- Spot and fix GSAP anti-patterns in existing components (animations triggered in render, missing cleanup, unscoped selectors)
- Reach for `useGSAP` + `contextSafe` correctly for interaction-driven animations (hover, click)
- Know when GSAP is overkill and a CSS transition would do
- Confidently refresh specific Next.js areas as they come up in real work, rather than a full curriculum re-run

## Constraints
- Solo freelancer, learning happens inline with real project work (this repo)
- Prefers fast, concrete lessons tied to actual files over generic tutorials

## Out of scope
- Full Next.js fundamentals re-teach — only refresh what real work surfaces as rusty
- Non-GSAP animation libraries (Framer Motion, etc.) unless it comes up
